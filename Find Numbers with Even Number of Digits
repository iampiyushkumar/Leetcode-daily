class Solution {
public:
    int findNumbers(vector<int>& nums) {
        int cnt=0;
        for(auto x:nums)
        {
            if(((int)(log10(x)) + 1) % 2 == 0)
              cnt++;
        }
        return cnt;
    }
};
